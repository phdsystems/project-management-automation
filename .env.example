# Git Platform Organization Configuration
# Copy this file to .env and fill in your values

# Your organization name (GitHub organization or Gitea organization)
ORG=my-org-name

# Backend platform: github (default) or gitea
# BACKEND=github
# BACKEND=gitea

# Optional: Override default configuration file
# CONFIG=project-config.json

# Optional: Override default branch
# DEFAULT_BRANCH=main

# Optional: Enable dry-run mode (1 = enabled, 0 = disabled)
# DRY_RUN=0

# Optional: Enable verbose output (1 = enabled, 0 = disabled)
# VERBOSE=0
